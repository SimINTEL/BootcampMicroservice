server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:database.sqlite
  jpa:
    database-platform: com.aprj.config.SQLiteDialect
    hibernate:
      ddl-auto: update
logging:
  file: springboot.log

